# Copy to .env and set values.
# Required:

HTTP_TIMEOUT=5
PORT=8000
LOG_LEVEL=INFO
PRICE_UPDATE_INTERVAL=5

# Optional — spread history (GET /v1/spread-history).
# If unset, the endpoint returns 503.
#
# With local Postgres: run  psql -d postgres -f scripts/init-local-db.sql
# With Docker:        run  docker compose up -d postgres
#
# DATABASE_URL=postgresql://spreads:spreads@localhost:5432/spreads
# SPREAD_HISTORY_INTERVAL_SECONDS=3600
